# `omena-cst-typed`

Borrowed typed CST projection over the parser-owned Omena CSS syntax tree.

This crate keeps `omena-parser` as the tree authority and exposes typed accessors
plus the generic parse-tree egress shape used by consumer surfaces.

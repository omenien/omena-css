# omena-transform-cst

`omena-transform-cst` owns the stable transform-layer contract for the
post-v5 omena-css track. It does not print or minify CSS yet. It defines the
pass DAG, the semantic/cascade proof obligations, and the provenance-preserving
artifact boundary that downstream transform crates will consume.

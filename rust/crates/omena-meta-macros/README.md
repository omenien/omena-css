# omena-meta-macros

Compile-time metadata attributes for Omena CSS spec and transform surfaces.

This crate is the M4 foundation for generated or pinned metadata. It validates
the shape of `#[spec(...)]` and `#[pass(...)]` annotations without making broad
spec-parity claims.

# omena-umbrella

Umbrella facade re-exporting the omena reusable building blocks (R1) and composed engine libraries (R2). Pure pub-use re-export layer; depend on this to access the omena public surface as `omena_umbrella::<crate>`.
